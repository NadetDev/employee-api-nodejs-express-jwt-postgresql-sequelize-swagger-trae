apiVersion: v1
kind: Service
metadata:
  name: employee-api
  namespace: employee-api
  labels:
    app: employee-api
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: employee-api